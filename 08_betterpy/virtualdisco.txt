$ python3 -m venv ~/myherozero

This command creates a virtual environment. -m is the notation for calling or using a module. venv is the module being used which creates a virtual environment. It is usually installed with python. ~/myherozero is the subfolder/user directory that is created in order to run any scripts in. 
https://docs.python.org/3/library/venv.html
